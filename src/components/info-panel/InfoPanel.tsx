export default function InfoPanel(props: { id: string }) {
  return (
    <>
      <div id={props.id} className="panel">
        <div className="panel-subsection">
          
        <button type="button">test</button>
        </div>
      </div>
    </>
  );
}

